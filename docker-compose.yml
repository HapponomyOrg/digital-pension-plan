version: "3.9"

services:
  websocket-proxy:
    build: .
    container_name: pensionplan-websocket
    restart: unless-stopped
    ports:
      - "8080:8080"
      - "4222:4222"
      - "6222:6222"
      - "8222:8222"
