websocket {
  # Enable WebSocket support
  listen: 0.0.0.0:8080

  # WebSocket connection settings
  no_tls: true  # Disable TLS for WebSocket (not recommended for production)
}

# Standard NATS configuration
server_name: mynats
listen: 0.0.0.0:4222
